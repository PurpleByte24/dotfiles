# Oh-My-Zsh settings that must be set before $ZSH/oh-my-zsh.sh is
# sourced. The ZSH= path itself is OS-specific: see pre-omz/05_omz_path.zsh.
ZSH_THEME="powerlevel10k/powerlevel10k"
CASE_SENSITIVE="true"

plugins=(
    git
    zsh-autosuggestions
    zsh-syntax-highlighting
    fzf-tab
)
